name: Deploy to Staging

on:
  push:
    branches: [staging]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy-staging:
    name: Deploy to Railway Staging
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: staging

      - name: Deploy to Railway Staging
        uses: bervProject/railway-deploy@main
        with:
          railway_token: ${{ secrets.RAILWAY_TOKEN }}
          service: ${{ secrets.RAILWAY_SERVICE_STAGING }}

      - name: Notify Success
        if: success()
        run: |
          echo "🚀 Deploy em STAGING concluído!"
          echo "🔗 URL: ${{ secrets.STAGING_URL }}"
          echo "📋 Validação será executada automaticamente"

      - name: Notify Failure
        if: failure()
        run: |
          echo "❌ Deploy em staging falhou!"
          echo "🔧 Verifique os logs do Railway"